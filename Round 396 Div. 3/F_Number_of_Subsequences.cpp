#include <iostream>
#include <array>
#include <vector>
#include <math.h>
#include <tuple>
#include <map>
#include <algorithm>
#include <string>

using namespace std;
typedef long long ll;

ll mod = 1e9 + 7;
ll n;
string s;

map<string, vector<ll>> m;

void calculate(string substr){
    for(int i = 0; i < s.length(); i++){
        if(substr.back() == i){
            m[""]
        }
    }
}
int main()
{
    cin >> n; cin >> s;
    
    return 0;
}